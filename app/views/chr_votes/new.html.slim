= render 'form'

- content_for :head do
  h3.sayfilter_heading Listing chr_votes

- content_for :info do
  =semantic_form_for chr_vote do |f|
    =f.input :comment
    =f.input :phase, collection: chr_vote_phases, include_blank: false
    =f.input :face_id, as: :hidden
    - if admin?
      label 自由入力
      input#phase_input(name='phase_input')

  - chr_vote_phases.each do |phase| chr_votes = chr_votes_in_phase[phase]
    h3= phase
    - chr_votes.each do |chr_vote|
      = image_tag("/images/portrate/#{chr_vote.face_id}.jpg")



