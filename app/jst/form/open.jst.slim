table(class="say {{option(f.targets, f.target).mestype}}" ng-show="form_show.find('open')")
  tr
    td.img
      img(ng-src="{{img_csid_cid(f.csid_cid)}}")
    td.field
      form(name="write_form")
        .msg
          .form-horizontal
            h3.mesname
              a
                | {{f.longname}}
              | &nbsp;({{potof.sow_auth_id}})
              | &nbsp; {{potof.role_names.join("、")}}
              span(ng-show="0 < potof.bonds.length")&nbsp;★絆

            .vote(form="ff.jst" ng-repeat="ff in f.votes")
            .mark(ng-bind-html-unsafe="error(ff)" ng-repeat="ff in f.votes")

          .form-inline
            .formpl_content
              p(class="{{f.style}} text" ng-bind-html-unsafe="preview(f)" ng-show="f.is_preview")
              textarea(cols="30" rows="5" ng-model="f.text" ng-hide="f.is_preview")

              .mes_date
                | {{option(f.targets, f.target).name}}
              a.btn(ng-click="f.is_preview = false" ng-show="f.is_preview") 戻る
              a.btn(ng-click="write(f, f.valid)") {{f.title}}

              select.input-medium(ng-model="f.target" ng-options="o.val as o.name for o in f.targets")
              select.input-mini(ng-model="f.style" ng-options="o.val as o.name for o in form.styles")
              p
                | {{f.caption}}
              span(class="{{f.error}}" ng-bind-html-unsafe="text_valid(f)")
              span(diary="f" ng-hide="f.is_preview")
              .mark(ng-bind-html-unsafe="error(f)")
