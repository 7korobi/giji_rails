//"日本語";
<%
%w[error giji option sow url].each do |name|
  const = name.upcase.to_sym
  if Kernel.constants.member?(const)
%>

this.<%= const %> = <%= Kernel.const_get( const ).to_json %> ;

<%
  end
end
%>

this.SOW_RECORD = {"CABALA":<%= SOW_RECORD[:CABALA].to_json %>};
