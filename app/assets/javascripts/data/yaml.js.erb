//"日本語";
<%
%w[error giji option sow url delay].each do |name|
  const = name.upcase.to_sym
  if Kernel.constants.member?(const)
%>

this.<%= const %> = <%= JSON.pretty_generate Kernel.const_get( const ) %> ;

<%
  end
end
%>

this.SOW_RECORD = {"CABALA":<%= JSON.pretty_generate SOW_RECORD[:CABALA] %>};
