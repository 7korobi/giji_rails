//"日本語";

<%
Dir.glob('lib/*.yml').each do |path|
  file, name = /(\w+).yml/.match(path).to_a
  const = name.upcase.to_sym
  if Kernel.constants.member?(const)
%>

<%= const %> = <%= Kernel.const_get( const ).to_json %> ;

<%
  end
end
%>
