tr
  td
    a(href="{{story.file}}")
      code.glyphicon.glyphicon-folder-open
    span.small
      | {{story._id}}
    a(href="{{story.link}}") {{story.name}}
    img(src="/images/icon/cd_{{story.rating}}.png")
